version: "2"
sql:
  - engine: postgresql
    queries: queries/*.sql
    schema: migrations
    gen:
      go:
        package: sqlc
        out: sqlc
        emit_interface: true
        emit_json_tags: true
        emit_prepared_queries: true
        emit_exact_table_names: false